# 实现细节存档（从 spec 输入中移出的内容）

> 用途：以下内容曾出现在 `speckit-specify-input.md` 中，因属于实现细节（HOW）已从 spec 输入移出或改写为行为描述。此处存档，供 plan 阶段实现参考，避免丢失。
> 存档时间：2026-08-04

## 1. 模拟推进机制（原概述、第 25 条）

- 原内容：底层固定 tick 的确定性模拟。
- plan 提示：模拟核心以固定 tick 步进，保证确定性、无头可测（对应宪法"无头可测"与"确定性裁决"原则）。

## 2. 配属接口设计（原第 10 条）

- 原内容：所有层级有统一接口 RequestSupport（目标、需求类型、支援种类）；上级检查资源池后 AssignTo（下级指挥官、单位），临时移交指挥权。
- plan 提示：接口命名与签名、资源池数据结构、指挥权转移标识、任务结束归建逻辑。

## 3. AI 调用不阻塞模拟（原第 26 条）

- 原内容：模拟不阻塞等待 AI，AI 命令异步到达并通过校验后执行。
- plan 提示：异步任务队列、命令校验管道、AI 超时与降级处理。

## 4. AI 决策延迟计入指令延迟（原第 30 条）

- 原内容：上级决策延迟（含 AI 思考时间）计入指令延迟。
- plan 提示：指令延迟 = 信息上送延迟 + 决策耗时（LLM 调用时间）+ 指令下达延迟；玩家指令同样适用。

## 5. 增量上下文（原第 40 条）

- 原内容：每次调用只发送"上次以来的变化 + 当前决策问题"，不发送全量状态。
- plan 提示：按 agent 维护状态增量；决策请求 = 增量 + 当前问题。

## 6. 上下文预算与历史记录（原第 41 条）

- 原内容：详细上下文有大小上限，超过上限自动压缩；系统保留有上限的历史记录文本，LLM 可查询指定时间点事件。
- plan 提示：上下文裁剪/压缩策略；历史日志存储上限与检索接口。

## 7. 任务状态机（原第 47 条）

- 原内容：已下达 → 执行中 → 完成/失败/被取消（状态机箭头表述）。
- plan 提示：状态枚举、状态转移事件、持续/循环任务的语义。

## 8. 指令格式 schema（原第 48 条）

- 原内容：指令格式字段：类型、目标、完成条件、意图/附加说明、优先级、时限。
- plan 提示：结构化指令数据定义（玩家与 AI 共用同一 schema），含校验规则。

## 9. agent 术语与架构表述（原第 51–56 条）

- 原内容：
  - 指挥树与 agent 树一一对应（父 agent / subagent）；
  - 参谋 agent 为节点的专属子 agent；
  - 上下文边界即信息权限；
  - agent 按事件触发实例化；
  - 同一指挥链串行决策、互不相关分支可并行、每阵营决策队列限流；
  - 连排级战斗无 subagent。
- plan 提示：agent 生命周期（惰性实例化）、上下文边界实现、决策调度（链内串行/分支并行/限流）、参谋角色实现。

## 10. AI 后端抽象（原第 71 条）

- 原内容：AI 接入抽象为 provider：云端 API、本地模型、脚本兜底可切换。
- plan 提示：统一 AI 后端接口；云端/本地/脚本三种实现；模型与提示词版本管理（对应宪法原则）。

## 待 plan 阶段补充的设计点

- 三大派系资源池数据模板的字段定义
- 任务类型清单（数据驱动、可扩展）
- 经验系统数值模型（受训程度/服役时长/战斗经验 → 各能力的具体公式）
- 地形四分类的数值影响（移动、射击、隐蔽）
- 伤害公式（穿深差、过穿、化学能上限、命中率、覆盖率）
- 失联概率与恢复的具体规则
- 失联恢复与压制恢复能力的数值关系
