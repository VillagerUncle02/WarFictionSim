namespace WarFictionSim.Tools;

/// <summary>工具 CLI 入口占位（数据校验 CLI 由 T069 实现）。</summary>
public static class Program
{
    public static int Main(string[] args)
    {
        Console.WriteLine("WarFictionSim.Tools —— 数据校验/工具 CLI（后续任务实现）");
        return 0;
    }
}
